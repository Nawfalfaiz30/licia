# Licia V23.1 — Build Fix

Based on Licia V23 Major Overhaul local final.

## Fixes
- `app/(app)/capture/page.tsx`: fixed invalid `if (...) const` syntax and ensured offline capture resolves `task` vs `note` mode correctly.
- `app/(app)/dashboard/page.tsx`: renamed the `pomodoro_sessions` query result from `focus` to `focusSessions` to avoid duplicate `focus` declarations.

## Validation
- `node scripts/verify.mjs`: PASS
- 97 TS/TSX files parser-checked, 0 syntax errors.
- No literal `\\n` sequences found in `app/globals.css`.
- No runtime `localhost:3000` / `http://localhost` references found in TS/TSX/JS/MJS/JSON/CSS source.

Run locally:
```
npm install
npm run preflight
npm run verify
npm run build
```
