# Licia V22.2 — Build Fix

## Fixed
- Corrected `app/globals.css` where a V22.1 mobile hardening block was accidentally written with literal `\\n` escape sequences instead of real newlines.
- Verified the CSS has no remaining literal `\\n` sequences.
- Verified CSS brace balance and parsed the stylesheet with `tinycss2` with 0 parse errors.

## Expected local validation
```powershell
npm run preflight
npm run verify
npm run build
```

## Important
The `npm install` output showing 2 vulnerabilities should not be resolved with `npm audit fix --force` yet. That can introduce breaking dependency changes unrelated to this build error. Audit and update dependencies separately after the app build is stable.
